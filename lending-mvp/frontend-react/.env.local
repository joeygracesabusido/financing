# Local Development (non-Docker) - use localhost to reach backend from host machine
VITE_API_URL=http://localhost:8000
# Backend uses REST API (FastAPI), not GraphQL
# VITE_GRAPHQL_URL=http://localhost:8000/graphql
