# Environment Configuration Template
# Copy this to .env.local and update values for your environment

# ─────────────────────────────────────────────────────────────────────────────
# DOCKER COMPOSE (Recommended for local development)
# ─────────────────────────────────────────────────────────────────────────────
# When running via docker-compose, use the service name 'backend' from 
# docker-compose.yml. This works because containers communicate via the
# 'lending_net' bridge network, not localhost.

VITE_API_URL=http://backend:8000
VITE_GRAPHQL_URL=http://backend:8000/graphql

# ─────────────────────────────────────────────────────────────────────────────
# LOCAL DEVELOPMENT (Non-Docker)
# ─────────────────────────────────────────────────────────────────────────────
# If running backend outside Docker on your machine:
# VITE_API_URL=http://localhost:8000
# VITE_GRAPHQL_URL=http://localhost:8000/graphql

# ─────────────────────────────────────────────────────────────────────────────
# PRODUCTION
# ─────────────────────────────────────────────────────────────────────────────
# VITE_API_URL=https://api.yourdomain.com
# VITE_GRAPHQL_URL=https://api.yourdomain.com/graphql
