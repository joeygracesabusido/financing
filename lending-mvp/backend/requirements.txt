# FastAPI & Server
fastapi
uvicorn[standard]

# GraphQL
strawberry-graphql[fastapi]

# Database — MongoDB (existing)
motor
pymongo

# Database — PostgreSQL (Phase 1 migration scaffolding)
asyncpg
sqlalchemy[asyncio]
alembic

# Cache & Sessions — Redis
redis[asyncio]

# Authentication
python-jose[cryptography]
passlib[bcrypt]==1.7.4
bcrypt>=4.0.1,<5.0.0
python-dotenv

# Configuration
pydantic-settings
pydantic

# Utilities
python-multipart
